# gitplus 作品介绍

> gitplus —— 本地优先的 AI 开发工作成果助手
>
> 让 AI 基于真实的 Git 记录帮你写提交信息、管理工作日志、生成智能周报，敏感信息不出本地，所有写操作必须人工确认。

---

## 1. 它解决什么问题

开发者每天都被三类"重复但重要"的工作消耗精力：

| 痛点 | 现状 | gitplus 的做法 |
| --- | --- | --- |
| 重复劳动多 | 理解改动、写规范的提交信息、整理周报，全靠人工复制归纳，耗时且容易遗漏 | 从 Git Diff、提交记录和工作日志中自动提取事实，AI 生成规范草稿 |
| AI 输出不可信 | 直接让大模型"凭空"写总结，容易产生幻觉，不敢用 | AI 输出严格建立在用户勾选的真实事实来源之上，且必须人工审阅确认 |
| 源码安全有顾虑 | 把代码交给远程大模型，存在凭证、密钥、内网信息泄露风险 | 调用远程模型前先在本地完成敏感信息扫描与脱敏，高风险内容默认阻断 |

一句话：**把"理解改动 → 写提交 → 记日志 → 出周报"这条每天都在走的链路自动化，同时守住安全和可信两条底线。**

## 2. 作品长什么样

gitplus 提供命令行和本地 Web 控制台双入口。Web 控制台只监听本机（127.0.0.1），通过一次性本地令牌建立会话，天然隔离外部访问。

### 2.1 Git 工作台

查看分支与文件状态，按文件暂存/取消暂存，受控查看 Diff，对改动执行安全扫描；确认无误后一键创建本地提交——所有写操作都要求用户明确确认。

> 📷 截图占位：Git 工作台页面（文件状态列表 + Diff 视图）

### 2.2 AI 提交助手

以当前改动和用户补充的上下文为依据，生成规范、简洁、可编辑的提交信息候选。生成前自动完成安全检查，生成后用户可修改主题和正文，确认后才落库为真正的 Commit。

> 📷 截图占位：AI 生成提交信息的编辑确认界面

### 2.3 安全扫描与脱敏

本地识别 API Key、访问令牌、JWT、明文密码、PEM 私钥、内网地址、邮箱手机号等敏感内容，按 critical / high / medium / low 分级：

- 高风险内容默认阻断远程模型调用；
- 可处理内容用稳定占位符脱敏（如 `<API_KEY_1>`），同一内容占位符一致，保护隐私的同时保留变更语义；
- 支持自定义正则规则和误报忽略，适配不同企业的保密要求。

> 📷 截图占位：安全扫描结果页面（风险分级列表）

### 2.4 工作日志

记录开发、排障、测试、评审等 Git 提交之外的真实工作，支持按时间、类型、标签、关键词筛选，既是个人复盘材料，也是周报的事实来源。

> 📷 截图占位：工作日志列表与新增表单

### 2.5 统一历史

把 Git 原始提交、gitplus 确认记录、手工工作日志汇聚到同一检索界面，支持多条件筛选、统计和 Markdown / JSON / CSV 导出。

> 📷 截图占位：统一历史检索页面

### 2.6 智能周报

采用"先选事实、再生成内容"的流程降低幻觉风险：

1. 选择周报日期范围；
2. 从统一历史中勾选本次采用的事实来源，可补充背景；
3. 以规则模式或 AI 模式生成含工作成果、问题处理、风险与后续计划的草稿；
4. 支持自然语言指令继续优化，也可直接编辑 Markdown；
5. 保存、确认、导出，已确认周报再编辑会自动回到草稿状态，保证版本可追溯。

> 📷 截图占位：智能周报生成与编辑页面

## 3. 亮点与创新

- **本地优先**：数据采集、安全分析、存储全部在本地完成，只有脱敏后的内容才可能发往远程模型；API Key 存放在独立 Secret 文件，不在接口和日志中回显。
- **事实驱动**：AI 不从零编造，所有生成内容都锚定在用户勾选的 Diff、提交和日志上，从机制上压制幻觉。
- **人工确认**：系统绝不自动执行 `git add` / `git commit` / `git push`，AI 结果和正式周报都必须由用户确认——AI 负责生成，人负责拍板。
- **规则兜底**：不依赖远程模型也能工作——mock Provider 支持离线演示，规则模式可完成基础周报整理，AI 是增强而非前提。
- **双入口**：CLI（Typer + Rich）面向工程化效率，Web 控制台面向可视化操作，同一套服务层，互不绑架。

## 4. 技术架构

```text
用户（CLI / 浏览器）
        │
        ▼
交互层：Typer CLI、FastAPI 路由、Jinja2 页面
        │
        ▼
应用服务层：提交、工作日志、历史、周报、配置、诊断服务
        │
        ├──────────────► AI 层：Provider 抽象、请求构建、响应解析
        ├──────────────► 安全层：扫描、分级、脱敏、远程调用拦截
        ├──────────────► Git 层：仓库状态、Diff、暂存、提交、日志读取
        └──────────────► 存储层：SQLAlchemy Repository、Unit of Work、SQLite
```

| 类别 | 技术选型 |
| --- | --- |
| 语言 | Python 3.9+ |
| Web 框架 | FastAPI + Jinja2 + 原生 JS/CSS |
| CLI | Typer、Rich |
| 数据校验 | Pydantic v2 |
| 存储 | SQLAlchemy + SQLite（本地优先） |
| 模型调用 | HTTPX，OpenAI-compatible / mock 双 Provider |
| 测试 | Pytest（单元 / 集成 / 端到端 / 性能） |

各层只依赖明确的领域模型和接口，方便单元测试，也方便后续替换模型服务或扩展数据来源。

## 5. 演示与使用方式

```bash
# 安装（要求 Python 3.9+ 和可用的 Git）
git clone <repository-url> && cd gitplus
python -m venv .venv
pip install -e ".[dev]"

# 环境自检
gitplus doctor

# 初始化并体验完整链路
gitplus init
git add <files>
gitplus check --staged          # 安全扫描
gitplus commit --provider mock  # 离线生成提交信息（无需模型服务）
gitplus worklog add --yes --type development --title "完成核心功能"
gitplus web                     # 打开本地 Web 控制台
```

仓库 `demo/` 目录提供一键演示脚本（`run_demo.ps1` / `run_demo.sh`），可自动创建演示仓库并走通完整流程。

> 🎬 录屏占位（可选）：从 gitplus web 启动到 AI 生成提交信息、生成周报的完整操作录屏，2~3 分钟

## 6. 质量保障

项目附带完整的工程化验证材料（见仓库 `artifacts/` 目录）：

- `test-report.xml`：单元 / 集成 / 端到端 / 性能测试结果
- `security-scan-report.md`：安全自查报告
- `performance-report.md`：性能报告
- `evaluation-report.md`：评估报告

配套文档见 `docs/` 目录：架构说明、安全说明、配置说明、Web 控制台使用、智能周报流程、开发指南等。

## 7. 边界与未来规划

当前边界（有意为之的设计取舍）：

- AI 结果仅作为辅助建议，必须人工审阅；
- SQLite 面向个人和小团队的本地使用场景，不做多人协作服务端；
- 安全规则可降低风险，但不承诺发现所有敏感内容；
- 不提供远程仓库同步、IDE 插件和复杂项目管理功能。

未来可演进方向：接入更多模型 Provider、扩展敏感信息规则库、探索 IDE 集成、支持团队级只读汇总视图。

---

> 完整技术细节请参考仓库内 [项目文档](gitplus/docs/项目文档.md) 与 [README](gitplus/README.md)。
