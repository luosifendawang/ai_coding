# Configuration

GitPulse will load configuration from defaults, user-level configuration, project `.gitpulse.yml`, environment variables, and CLI options.

Secrets such as API keys and Feishu App Secrets must be read from the local secret file or environment variables.
