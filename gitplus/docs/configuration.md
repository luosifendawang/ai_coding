# Configuration

gitplus will load configuration from defaults, user-level configuration, project `.gitplus.yml`, environment variables, and CLI options.

Secrets such as API keys and Feishu App Secrets must be read from the local secret file or environment variables.
