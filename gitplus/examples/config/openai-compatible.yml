# Remote OpenAI-compatible provider. Store the real key only in the environment.
ai:
  provider: openai-compatible
  base_url: https://api.example.test/v1
  model: example-model
  api_key_env: GITPLUS_API_KEY
  is_local: false
security:
  block_remote_on_high_risk: true
